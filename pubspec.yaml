name: silky_scroll
description: "Silky Scroll is a package developed in Flutter to provide a smooth scrolling experience."
version: 1.0.16
homepage: https://github.com/Dynaruid/silky_scroll

environment:
  sdk: ^3.8.1
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  provider: ^6.1.5
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

platforms:
  android:
  ios:
  linux:
  macos:
  windows:
  web: