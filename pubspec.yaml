name: silky_scroll
description: "Silky Scroll은 Flutter에서 부드러운 스크롤 경험을 제공하기 위해 개발된 패키지입니다."
version: 1.0.0
homepage: https://github.com/Dynaruid/silky_scroll

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  web: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
